mutation Login($phone: String!, $password: String!) {
  login(phone: $phone, password: $password) {
    token
    user {
      id
      uid
      name
      phone
      role
      isBlocked
      companyId
      storeIds
      assignedStoreId
      createdAt
      updatedAt
    }
  }
}






